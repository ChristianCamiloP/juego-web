import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import App from "./App";

// Renderizamos la aplicación en el contenedor raíz
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
